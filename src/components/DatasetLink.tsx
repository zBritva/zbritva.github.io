

import React from 'react'
import ReactPlayer from 'react-player'


export interface IIconProps {
    url: string;
    alt: string;
    size?: number;
}

export const DatasetLink = ({url, alt, size}: IIconProps) => {
    return (
        null
    );
};



